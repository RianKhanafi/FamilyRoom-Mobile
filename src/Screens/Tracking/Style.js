import {StyleSheet} from 'react-native';

const Style = StyleSheet.create({
  header: {
    position: 'relative',
    zIndex: 999999,
  },
  card: {
    elevation: 5,
    borderRadius: 20,
    borderColor: '#fff',
    overflow: 'hidden',
  },
});

export default Style;
